import React, { Component } from 'react';
import classNames from 'classnames';

class Base extends Component {
    render() {
        return (
            <div>
            </div>
        );
    }
}

export default Base;